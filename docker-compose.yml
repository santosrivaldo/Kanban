services:
  kanban:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      PORT: 3000
      # Use X_TOKENS do .env ou variável de ambiente; senão usa o valor abaixo
      X_TOKENS: ${X_TOKENS:-sprint-token-2025}
